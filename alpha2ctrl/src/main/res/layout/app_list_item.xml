<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:descendantFocusability="blocksDescendants"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="5dp" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="4" 
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/app_logo"
            android:layout_width="@dimen/app_image_size"
            android:layout_height="@dimen/app_image_size"
            android:layout_centerHorizontal="true"
            android:layout_centerInParent="true"
            android:src="@drawable/no_app"
            android:background="@drawable/app_unselected"
            android:padding="3dp"
            android:scaleType="fitXY" />

         <ImageView
             android:id="@+id/app_delete_flag"
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:layout_alignRight="@id/app_logo"
             android:layout_alignTop="@id/app_logo"
             android:visibility="gone"
             android:background="@drawable/delete_flag" />

    </RelativeLayout>

    <TextView
        android:id="@+id/app_name"
        android:textSize="15dp"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:singleLine="true"
        android:layout_gravity="top|center_horizontal"
        android:layout_weight="1"
        android:gravity="center_horizontal"
        android:textColor="#323232" />

</LinearLayout>